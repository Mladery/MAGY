# 类脑娘 · 常见问题 Wiki

> 维护说明：本站 Wiki 内容托管在 GitHub 仓库的 `wiki/` 目录，服务器会定时拉取这些 Markdown 文档并渲染展示。
> 提问前请先通读本页所有标注（Q1~Q5 及常见报错），大部分问题都能在这里找到答案。

## 目录

- [Q1：400 报错 "Requests ending with a model turn are not supported."](#q1)
- [Q2：400 报错 "User location is not supported for the API use."](#q2)
- [Q3：凭证上传失败怎么回事？](#q3)
- [Q4：403 报错 "The caller does not have permission"？](#q4)
- [Q5：注册码哪里有？又在哪里用？](#q5)
- [常见报错与提示](#常见报错与提示)
- [提问格式](#提问格式)
- [最近更新](#最近更新)

---

## Q1：400 报错 "Requests ending with a model turn are not supported." 怎么办？ {#q1}

**根本原因**：你发送的文本中最后一条对话是以 AI 回复结尾（assistant）。

**解决办法**：

- 再发一条 `continue` 或一个空格即可；
- 或者编辑对话，让最后一条不是 assistant。

---

## Q2：400 报错 "User location is not supported for the API use." 怎么办？ {#q2}

**根本原因**：公益站的代理轮询到了 Google 不支持的落地 IP 节点。

**解决办法**：这不是你的问题，只能重新 roll（重摇一次）。

---

## Q3：凭证上传失败怎么回事？ {#q3}

1. **请不要把 GCLI 里的凭证上传给这里的公益站！AGY 公益站不收 GCLI 凭证！**
2. GG 公益站里的"下载凭证"全部都是 GCLI。
3. 请优先使用 **OAuth 授权**方式提交凭证。
4. 出现 `missing project_id` 报错？我们已经进行过修复；如果多次尝试仍然失败，可带图提问，但有概率无法处理。
5. 上传凭证有概率导致账号被封，请慎重选择上传；可能导致的后果是 Google 账号的 AGY 和 GCLI 一起被 ban。

---

## Q4：403 报错 "The caller does not have permission" 怎么办？ {#q4}

**根本原因**：凭证池内有人上传了不支持 AGY 的凭证并且没有被验死。

**解决办法**：不属于你的问题，重新 roll 就好了。

---

## Q5：注册码哪里有？又在哪里用？ {#q5}

- **获取渠道**：
  1. 随机、不定期由站主发放；
  2. 《恶魔轮盘赌》0 配额场有概率掉落注册码（可在子区聊天中找人组局）。
- **使用位置**：DC 授权后，如果你没有注册，页面会提示你输入注册码。
- **复制了但报错？**
  1. 隐藏注册码是 `||注册码||`：你点开后看到的是注册码，但实际复制的内容包含两端的 `||||`，需要删除 `||` 后再注册。
  2. 注册码被用完了，蹲下次发放。

---

## 常见报错与提示 {#常见报错与提示}

### 拉不出模型？重 roll 一直报错？

1. URL 加 `/v1` 了吗？key 复制对了吗？换成非流式试试。
2. 预设是否适配模型？聊天记录有没有问题？多换几个试试。
3. 排除以下参数：
   - `presence_penalty`
   - `frequency_penalty`
   - `top_p`
   - `top_k`
   - `temperature`
4. 提示 `Requests ending with a model turn are not supported.`？
   - 发送文本中最后一条对话为 AI 回复结尾（assistant），打个 `continue` 或空格即可，或者编辑对话。
5. `gemini-3.1-flash-image` 调用报错？
   - 请将 URL 后缀改为 `/v1/images/generations`。

### 凭证相关补充

- 请优先使用 OAuth 授权提交凭证。
- 不要把 GCLI 凭证上传给 AGY 公益站。

---

## 提问格式 {#提问格式}

1. **酒馆 RP 相关问题**：请一并附上：酒馆插件、报错日志截图/粘贴。
2. **凭证上传相关问题**：请截图首页上提示的信息。

---

## 最近更新 {#最近更新}

1. **Opus 系列模型 403 缓解 & 特殊配额上线**
   - 原因：经核实只有 Pro 凭证才可以调用 Opus 系列模型，已在凭证池收缩调用范围，大幅缓解 403 错误。
   - 调整：Opus 系列模型消耗的 Claude 配额为 5 倍消耗。
2. **轮盘赌页面重写 & 音乐上线**
3. **轮盘赌 0 配额消耗模式上线 & 扩大可玩身份**
   - 扩大可玩身份：不需要站内注册码，只要你是类脑社区的就能玩。
   - 0 配额模式上线：没有下注，场外的玩家也匹配这个池子。
   - 特殊：可能会引发一些好运哦。