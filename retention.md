# 账号保留与清退

本站按 **7 天为一个周期**判定账号去留，每周自动检查一次。每个周期内**满足以下任意一条**即可再留一个周期：

1. **用本站发给你的 API Key 成功调用过模型**（本周期内，任意一次、任意模型）；
2. **提供过可用凭证**（本周期内通过 OAuth 授权或上传，验活成功）。

两条是**或**的关系，满足其中一条就够。两条都不满足的账号会被清退 —— 把有限的凭证额度留给真正在用的人。

## 什么时候清退？

- **时间**：每周五 **23:00（北京时间）** 自动执行一次；如果那一刻服务器不可用则跳过，**不会补跑**。
- **对象**：注册后**一个周期内既没调用过模型、也没能提供可用凭证**，且能被用量记录证实的账号。查不到记录的历史账号不会被清退。
- **动作**：该账号的 API Key 立即失效，名下上传的凭证文件一并从共享池删除、**不保留**。
- **想回来**：需要**重新获取注册码**并兑换后，才能再次使用本站（旧注册码已作废，不能再用）。

> 判定看的是"**这个周期有没有真的在用**"，不是"有没有注册过"。只领了注册码、只登录过门户、只用过别的站点的 Key，都不算。

## 什么情况会保留？

| 情况 | 结果 |
| --- | --- |
| **本周期内**用本站 Key 成功调用过模型 | ✅ 保留**至本周期结束** |
| **本周期内**提供过可用凭证（OAuth 授权或上传，验活成功） | ✅ 保留**至本周期结束** |
| 以上两条都不满足，但注册不满 **7 天** | ⏳ 自动缓冲，按各自注册时间单独计算到期日 |
| 兑换过注册码 | ⚪ 不构成保留理由（领码只代表注册成功，仍需真正用过） |
| 注册时间早于本站统计起点、无法确认是否用过 | ❔ 状态未知，**不会被清退**（按留存处理） |

> 清退只针对**已证实从未使用**的账号。统计台账覆盖不到的历史账号一律按留存处理，不会因为"查不到记录"被删。

## 每个周期都重新判定

保留**不是永久**的。本周期因为调用过模型或提供过凭证而被留下，**下个周期照旧要重新判定**：

- 上个周期调用过模型、这个周期一次都没调用 → 会被清退（凭证也一样，上个周期提供过、这个周期没有可用凭证 → 会被清退）；
- 想长期留下，就保持**每个周期内至少有一次成功调用**，或者持续提供可用凭证。

凭证在中途失效（例如被上游限制）**不影响本周期**的保留 —— 只要本周期内它被验活成功过。

## 门户上的留存状态标记

门户账号区块会直接显示当前状态，不必猜：

| 标记 | 含义 |
| --- | --- |
| ✅ 已留存（本周期内调用过模型） | 保留至本周期结束，下个周期重新判定 |
| ✅ 已留存（提供了可用凭证） | 保留至本周期结束，下个周期重新判定 |
| ⏳ 缓冲期中 | 注册不满 7 天，到期后按上面的规则判定 |
| ❔ 状态未知 | 注册早于统计起点，无法确认是否用过；**不会被清退** |
| ❌ 未留存 | 将在下一次清退时移除 |

## 我明明用过模型，为什么还是被清退了？

请先确认这几点：

1. **时间**：那次调用是不是发生在**上一个周期**（超过 7 天前）？本周期内需要至少再成功调用一次。
2. **Key**：调用时用的是**本站门户发给你的那把 API Key**（`sk-` 开头），不是其他站点的 Key；Key 重新生成过的话，要确认新 Key 成功请求过。
3. **成功**：请求**真正生成过内容**。全部失败的请求（401 / 403 / 配额耗尽 / 被风控拦截）不计入"调用过"。

仍有异议请带 **Discord 账号 + 大致调用时间 + 请求记录截图**反馈，管理员可以核查台账。

## 为什么要有清退？

共享凭证池的额度来自社区成员的账号。长期不用的账号会占用注册名额，也让活跃度统计失真。清退只针对**已证实本周期内没有使用**的账号 —— 每个周期内保持一次成功调用（或持续提供可用凭证）就能一直留在本站。
