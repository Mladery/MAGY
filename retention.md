# 账号保留与清退

本站按 **7 天为一个周期**，每周自动检查一次账号是否还在使用。**注册后从未调用过模型、也没有提供过可用凭证的账号会被清退**，把有限的凭证额度留给真正在用的人。

## 什么时候清退？

- **时间**：每周五 **23:00（北京时间）** 自动执行一次；如果那一刻服务器不可用则跳过，**不会补跑**。
- **对象**：注册后**一次都没有成功调用过模型**的账号。
- **动作**：该账号的 API Key 立即失效（不再是本站有效 Key），名下上传的凭证文件一并从共享池删除，**不保留**。
- **想回来**：重新打开门户完成 Discord 授权即可，会拿到一把**新的 API Key**（旧 Key 不会恢复）。

> 清退看的是"**有没有真的用过**"，不是"有没有注册过"。只领了注册码、只登录过门户、只用过别的站点的 Key，都不算用过。

## 什么情况会保留？

| 情况 | 结果 |
| --- | --- |
| 用本站发给你的 API Key **成功调用过模型**（任意一次、任意模型） | ✅ **长期保留**，之后不再参与清退 |
| **本周期内提供过可用凭证**（OAuth 授权或上传，验活成功） | ✅ 保留**至本周期结束**，下个周期重新判定 |
| 注册不满 **7 天** | ⏳ 自动缓冲，按各自注册时间单独计算到期日 |
| 兑换过注册码 | ⚪ 不构成保留理由（领码只代表注册成功，仍需真正用过） |
| 注册时间早于本站统计起点、无法确认是否用过 | ❔ 视为状态未知，按运营策略一并清退 |

**关于凭证保留（重要）**：它**不是永久**的。本周因为你提供了可用凭证而被留下，下个周期会**重新判定** —— 仍然需要有"调用过模型"或"本周期内又提供了可用凭证"，否则会在下个周期的清退中移除。凭证在中途失效（例如被上游限制）**不影响本周期**的保留。

## 门户上的留存状态标记

门户账号区块会直接显示当前状态，不必猜：

| 标记 | 含义 |
| --- | --- |
| ✅ 已留存（调用过模型） | 长期保留，不再参与清退 |
| ✅ 已留存（提供了可用凭证） | 本周期保留，下个周期重新判定 |
| ⏳ 缓冲期中 | 注册不满 7 天，到期后按上面的规则判定 |
| ❔ 状态未知 | 注册早于统计起点，无法确认是否用过 |
| ❌ 未留存 | 将在下一次清退时移除 |

## 我明明用过模型，为什么还是被清退了？

请先确认这几点：

1. 调用时用的是**本站门户发给你的那把 API Key**（`sk-` 开头），不是其他站点的 Key；
2. 请求**真正生成过内容**。全部失败的请求（401 / 403 / 配额耗尽 / 被风控拦截）不计入"调用过模型"；
3. Key 重新生成过的话，新 Key 至少成功请求过一次（统计以**实际发出的 Key** 为准）。

仍有异议请带 **Discord 账号 + 大致调用时间 + 请求记录截图**反馈，管理员可以核查台账。

## 为什么要有清退？

共享凭证池的额度来自社区成员的账号。长期不用的账号会占用注册名额、也让"活跃度"统计失真；清退只针对**确认从未使用**的账号，任何一次成功调用都会让你永久留在本站。
