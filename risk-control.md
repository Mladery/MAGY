# 风控与账号安全

本页说明 MAGY 公益站的风控规则。公益站资源有限，为了防止脚本与贩子破坏社区体验，网关会自动执行以下风控措施。

> 如果你被误伤，请带账号信息联系管理员人工处理。

## 一、全局限流

- 按 **来源 IP** 与 **登录账号** 分别做每秒请求限制（QPS）。
- 登录、OAuth 授权、凭证上传、注册码提交、吊销 Key 等**敏感操作**配额更严。
- 触达上限返回 429，请放慢速度重试。

## 二、敏感失败与临时限制

以下操作连续失败会被追踪（10 分钟窗口）：

- 注册码输入错误
- OAuth 授权提交 / 验活失败
- 凭证验活失败

达到阈值后，该 IP / 账号会被**临时限制约 10 分钟**，到期自动恢复。

## 三、防贩子规则（直接封禁）

为了对付倒卖注册码 / API Key / 凭证的灰产行为，以下行为会触发**永久封禁 DC 账号或 IP**（封禁提示会写明具体原因）：

1. **同一 IP 关联过多 DC 账号**：判定为养号农场，封禁该 IP。
2. **同一个 DC 账号囤积多个注册码**：正常用户只会兑换一次注册码，多次成功兑换判定为囤码倒卖，直接封禁该 DC 账号。
3. **同一个 DC 账号从大量不同 IP 登录**：判定为账号共享 / 被转卖，触发风控观察（视情况升级为封禁）。

### 封禁后怎么办

- 封禁写入站点封禁名单，所有页面均拒绝访问，页面会说明**具体触发的规则**（钓鱼粘贴 / 养号农场 / 囤码倒卖 / 多 IP 共享）。
- **被永久封禁的账号，其 API Key 会同时被吊销**：文字生成、图片生成等接口调用会立即失效，不只是打不开网页。想恢复必须联系管理员人工解封，解封后重新登录门户即可获得新 Key。
- 如果你认为是被误封，请带上：
  - Discord 用户名 / ID
  - 你正常使用的时间与场景
- 联系管理员人工核实。

## 四、钓鱼陷阱提示

站内注册码以 ||注册码|| 剧透格式发布。**复制时请务必删掉首尾的 ||**。如果提交的注册码仍带 ||，系统会判定为脚本原样粘贴，按次数封禁账号 + IP：

- **第一次**：封禁 **24 小时**，页面会显示具体解封时间，到期自动恢复。
- **第二次及以后**：**永久封禁**，不再自动解除。

> 提醒：这是按「账号 / IP」分别计次的，同一个账号或同一个 IP 再次触发都会升级为永久封禁。

## 五、安全建议

- 不要出租、共用你的 Discord 账号。
- 不要把自己的 API Key / 凭证交给他人。
- 不要在多个设备 / 代理 IP 上频繁切换登录同一个账号。
- 凭证上传有封号风险，请优先使用 OAuth 授权并谨慎上传。
